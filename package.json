{
  "name": "hn-colors",
  "version": "1.0.0",
  "description": "",
  "main": "colors.js",
  "scripts": {
    "start": "node colors",
    "zip": "rm -f build.zip && zip -r build.zip colors.js node_modules package.json package-lock.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ben Schulman",
  "dependencies": {
    "axios": "^1.4.0",
    "dotenv": "^16.3.1"
  }
}
